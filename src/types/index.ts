/**
 * TypeScript types and interfaces barrel export.
 *
 * Domain type definitions shared across the frontend.
 * Mirror backend entities to ensure type consistency.
 *
 *   user.types.ts    — User, UserRole
 *   farm.types.ts    — Farm
 *   crop.types.ts    — Crop, CropStatus
 *   plot.types.ts    — Plot
 *   api.types.ts     — ApiResponse<T>, PaginatedResponse<T>
 *
 * Example exports:
 *   export type { User, UserRole } from './user.types'
 *   export type { Farm } from './farm.types'
 *   export type { ApiResponse, PaginatedResponse } from './api.types'
 */

/**
 * Utilities barrel export.
 *
 * Pure helper functions — no side effects, no React imports.
 *
 *   cn.ts           — clsx + tailwind-merge utility (used by Shadcn/ui)
 *   formatters.ts   — date, currency, and number formatters
 *   validators.ts   — Zod schema helpers
 *   constants.ts    — app-wide constants
 *
 * Example exports:
 *   export { cn } from './cn'
 *   export { formatDate, formatCurrency } from './formatters'
 */

export { cn } from './cn';
